package com.sfmc.customhtmliam

import android.app.Activity

/**
 * Empty Activity used only as an overlay host in instrumented tests, so the library's androidTest
 * suite does not depend on any host-app Activity. Registered in the androidTest manifest.
 */
class TestHostActivity : Activity()
